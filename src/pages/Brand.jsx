import {useState} from 'react'; import {Check,Save} from 'lucide-react'; import {audiences,brandStyles} from '../mocks/data'; import {useApp} from '../context/AppContext'; import {Badge,Button,ChoiceGrid,Field,SelectField} from '../components/ui';
export default function Brand(){const {brand,updateBrand}=useApp();const [data,setData]=useState(brand);const [saved,setSaved]=useState(false);const set=(k,v)=>setData(d=>({...d,[k]:v}));const toggle=(k,v)=>set(k,data[k].includes(v)?data[k].filter(x=>x!==v):[...data[k],v]);const save=()=>{updateBrand(data);setSaved(true);setTimeout(()=>setSaved(false),2200)};return <div className="page brand-page"><header className="page-heading split"><div><Badge>IDENTIDAD DE MARCA</Badge><h1>Mi marca</h1><p>Mantén al día la esencia que guía todas tus recomendaciones.</p></div><Button onClick={save}><Save/>Guardar cambios</Button></header><div className="brand-editor"><section><div className="editor-block"><span>01 · TU NEGOCIO</span><div className="form-grid"><Field label="Nombre del negocio" value={data.name} onChange={e=>set('name',e.target.value)}/><Field label="Servicio" value={data.service} onChange={e=>set('service',e.target.value)}/><Field label="Ubicación" value={data.location} onChange={e=>set('location',e.target.value)}/><SelectField label="Idioma" value={data.language} onChange={e=>set('language',e.target.value)}><option>Español</option><option>English</option><option>Español e inglés</option></SelectField></div></div><div className="editor-block"><span>02 · TU PÚBLICO</span><ChoiceGrid items={audiences} selected={data.audiences} multiple onToggle={v=>toggle('audiences',v)}/></div><div className="editor-block"><span>03 · TU ESTILO</span><ChoiceGrid items={brandStyles} selected={data.styles} multiple onToggle={v=>toggle('styles',v)}/><div className="color-fields"><label className="field"><span>Color principal</span><div className="color-input"><input type="color" value={data.primary} onChange={e=>set('primary',e.target.value)}/><code>{data.primary}</code></div></label><label className="field"><span>Color secundario</span><div className="color-input"><input type="color" value={data.secondary} onChange={e=>set('secondary',e.target.value)}/><code>{data.secondary}</code></div></label></div></div></section><aside className="brand-mini-preview" style={{background:data.primary}}><small>ASÍ TE VERÁN</small><h2>{data.name}</h2><p>{data.description}</p><span style={{background:data.secondary}}>{data.styles[0]||'TU ESTILO'}</span></aside></div>{saved&&<div className="toast" role="status"><Check/>Tu marca quedó actualizada</div>}</div>}
